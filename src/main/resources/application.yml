server:
  port: 9090
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost/user_Management_System
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
    
    UMAP1:
     messages: 
       saveSuccess = contact saved successfully
       saveFail = Contact Failed to save
       Deleted = contact Deleted successfully
       NotDeleted = contact Not Deleted
 
